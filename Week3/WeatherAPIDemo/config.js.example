// Copy this file to `config.js` in the same directory and replace the placeholder
// with your real API key. The project .gitignore ignores `config.js` so your
// private keys won't be committed.

// Example content for config.js (DO NOT commit your real key):
// window.API_KEY = 'your_real_openweather_api_key_here';

// Replace the line below with your actual key when you create config.js
window.API_KEY = 'REPLACE_WITH_YOUR_API_KEY';
